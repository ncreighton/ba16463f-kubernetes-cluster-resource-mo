# ba16463f-kubernetes-cluster-resource-mo
A VS Code extension for platform engineers and DevOps teams providing real-time Kubernetes cluster visibility directly in the IDE. Shows pod status, resource requests vs limits, node pressure indicators, and namespace cost breakdowns without switching to a browser. Includes kubectl command shortcuts, YAML validation with best-practice linting, reso
